<%= form_for ([@store,@menu_section]) do |f| %>

        <% if @menu_section.errors.any? %>
                <div id="error_explanation">
                        <h2>
                                <%= pluralize(@menu_section.errors.count, "error") %> prohibited
                                this store from being saved:
                        </h2>
                        <ul>
                                <% @menu_section.errors.full_messages.each do |msg| %>
                                        <li><%= msg %></li>
                                <% end %>
                        </ul>
                </div>
        <% end %>

        <p>
                <%= f.label :name %><br>
                <%= f.text_field :name %>
        </p>

        <p>
                <%= f.submit %>
        </p>
<% end %>
